讀檔 > 圖片統一讀取路徑
寫檔 > 圖片統一寫路徑

地圖 > 讀取統一寫檔名(地圖放在Information)


讀取 > mapTxt > Editer > CBlock > 寫入ImageInfo
寫檔 > ImageInfo 寫入 CBlock > 寫入TXT

re load information:

(目前有幾張地圖)

(哪幾張地圖要ReLoad)
(多個數字 每個數字代表要Re Load的地圖的編號，一個地圖一行)